# Signs JWT session tokens. Set this to a long random string in any real
# deployment -- the fallback below is fine for local demo use only.
JWT_SECRET=dev-only-secret-change-me

# Optional: only needed if/when an LLM-driven agent mode is enabled.
# The heuristic agent (default) requires no API key at all.
ANTHROPIC_API_KEY=
